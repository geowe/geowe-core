function autocomplete(address){alert("address: "+address+"-----"+document.getElementById('autocompletar').value);};function getLatLongByAddress(address){var geocoder=new google.maps.Geocoder();geocoder.geocode({'address':address},function(results,status){if(status==google.maps.GeocoderStatus.OK){window.updateMap(results[0].geometry.location.lat(),results[0].geometry.location.lng());}else{window.finishGeocoding();alert("Geocode was not successful for the following reason: "+status);}});}